Install gems (tty-tables & tty-font) with:
`bundle install`

Change mode of the progressions script, in case it does not have execution permissions:
`chmod +x ./progressions`

START APP WITH:
`./progressions`